- 2026-07-23: 下一阶段演进方向固定为 NEXT_EVOLUTION_PLAN.md 的 E1-E5。优先级排序：E2 端到端验证 > E3 UI Loop Activity 集成 > E4 CCX 启动服务 > E1 Loop Decide 深度消费 > E5 配置覆盖闭环。理由：E2 是后续演进的基线，E3 是产品闭环最后一公里，E4 降低启动门槛，E1/E5 是增量优化。
# DECISIONS

- 2026-07-28: codex app-server initialize 超时独立配置（90s），不复用 handshake 30s。理由：codex state DB backfill 冷启动需 30s+，handshake 30s 必超时；initialize 是一次性冷启动握手，thread/start 和 turn/start 是热路径 30s 足够。两者均可通过系统属性覆盖（agentcloud.providers.codex.initialize_timeout_ms / handshake_timeout_ms）。

- 2026-07-28: recovery（prepareFreshSessionRecovery）必须重置 blocked subgoal 为 pending。理由：autoUpdateSubgoalStatus 只迁移 in_progress/pending 的 subgoal，blocked 是终态之一；recovery 不重置则 worker round 成功也无法标 done，任务永远卡 human_gate。同时清除 failure_summary_readable 避免过时错误信息残留。

- 2026-07-28: codex app-server 非 JSON 行不直接当输出，先过滤内部诊断日志。理由：codex 会向 stdout 混入 ANSI 着色的 ERROR/WARN 内部日志（如 ReasoningSummaryDelta without active item），这些不是任务输出但会被 appendOutput 混入 last_message.md 和 summary。isCodexInternalLog 过滤 ANSI+ERROR/WARN 和时间戳前缀日志行，只保留真正的非 JSON 输出。

- 2026-07-28: workspace 定位完全 provider-driven，harness 不做文本推断。codex cwd 解析只接受显式 workspace_root（操作者/API 传入）或中性目录回退（alias 公共父目录），不再用 alias 子串匹配从 task 文本猜仓库。理由：子串匹配对分析/日志类任务误命中（日志含项目名就把 cwd 拉到该仓库），且 codex 收到 prompt 仓库清单后完全能自行 cd 定位；harness 只提供事实（仓库清单）+ 守边界（不静默回退 harness 自身仓库），理解和定位交给执行 agent。路由关键词表（task_type 推断）保留为 worker 选择兜底，不影响 cwd。详见 docs/LLM_TASK_UNDERSTANDING_PLAN.md。

- 2026-07-28: 控制图 enter 异步化。createTask/continueTask 不再在 HTTP 虚拟线程同步执行 controlGraph.enter，改为虚拟线程异步 + per-task 锁防并发重跑；HTTP 立即返回。理由：worker round 长耗时（数百秒）会触发 HTTP 超时 kill 线程，导致 worker_round 事件与 task 状态丢失（codex 跑完但 harness 没记录）。测试通过 sync_enter 开关走同步保证控制图语义可断言，生产默认异步。

- 2026-07-24: CCX 与 Harness 双向对接使用 harness 专属模型名（harness / harness-strong / harness-fast），不复用 codex / codex-free / gpt-5.4 等公共模型名。理由：公共模型名会在 CCX 多渠道间负载均衡，导致请求被转发到其他上游而非 harness；专属模型名确保 CCX 调用 harness 时只命中 harness-chat / harness-responses 渠道。harness 启动时仍使用 codex 作为 OPENAI_MODEL，这是 harness → CCX → 实际 LLM 的方向，与 CCX → harness 方向互不干扰。harness 默认端口固定为 9090，与 CCX config 中配置的 harness-chat / harness-responses 渠道 baseUrl 保持一致。

- 2026-07-23: CCX 渠道配置不加新渠道，只在现有渠道的 modelMapping 里加 codex-free -> 实际模型 映射。理由：CCX 的设计是输入模型名后各渠道按 modelMapping 路由，新增渠道意味着重复配置 baseUrl/apiKeys/modelCapabilities。chatUpstream 和 responsesUpstream 都要配，因为 codex CLI 走 responses API，harness 直接 LLM 调用走 chat completions API。
- 2026-07-23: harness 只用两个 codex CLI lane（codex-main + codex-free），不用更多。理由：CCX 负责模型路由，harness 只需要区分"付费"和"免费"两个入口；CCX 背后是哪个渠道由 CCX 自己决定，harness 不关心。
- 2026-07-23: harness 配置采用 Sublime 式"默认 + 用户覆盖"模式。harness 自动维护 harness-state.json（本机状态），用户通过 harness-config.yml 覆盖（启用/禁用 provider、调整优先级）。合并规则：用户配置覆盖自动发现，未声明的用自动发现结果。

- 2026-07-23: Worker lane 注册采用配置驱动（harness-config.yml），不硬编码在 BuiltinAgentProviders.defaults() 中。理由：CCX 渠道变更频繁（新增/暂停/恢复），硬编码意味着每次改 Java + 重新构建 + 重新部署；配置驱动只需改 yml + 重启。harness-config.yml 是现有 workers.yml 的超集，第一版两者并存，配置不存在时回退到内置默认值。
- 2026-07-23: harness-config.yml 选 YAML 不选 JSON。理由：harness 已有 workers.yml 的 YAML 解析传统，YAML 支持注释适合人工维护，与 CCX 的 config.json（机器生成）分工明确。
- 2026-07-23: 免费 worker lane 统一标记 cost_class: free_auto，复用现有 ree_first 路由逻辑，不新写路由代码。免费 lane 仍是 providerId: codex 的 profile lane，不引入新 provider。
- 2026-07-23: CCX 渠道健康检查第一版只做启动时 precheck + 手动 refresh，不做自动 re-ready / 自动同步。理由：渠道状态变更频率低，自动同步增加复杂度但收益有限。

- 2026-07-22: P2 端到端集成验证已完成并固定决策口径：CCX 网关（`127.0.0.1:3688`）作为 harness 唯一 LLM 接入层，harness 通过 `-DOPENAI_BASE_URL` / `-DOPENAI_API_KEY` 系统属性指向 CCX；codex worker 通过 CCX 路由到实际 LLM（glm-4-flash）；端到端验证证明 `goal -> plan -> execute -> judge -> decide` 闭环在真实 LLM 调用上完整跑通，goal progress auto-update 和 last_loop_tick 在真实执行中生效。后续端到端验证继续使用 `Run-CcxIntegrationPrecheck.ps1` 作前置检查、`Run-HarnessWithJava21.ps1` 作 harness 启动入口、通过 CCX 路由名 `codex` 让 CCX 自动选择最佳上游；不要直接指定未路由的模型名（如 `glm-5.2` 直连 503）。


- 2026-07-22: Loop 验收标准 #2 的稳定产品决策固定：`resolveAction` 中 goal progress 判断优先于单轮 execution result 判断。具体规则：blocked subgoal -> human_gate（无论 execution 说什么）；all subgoals done -> done（无论 execution 说什么）；open subgoals + execution done -> checkpoint（保存进度但不标记完成，避免在 goal 未完成时误判 task 为 done）；无 subgoal_status 时保持原有行为不变。同时固定 goal progress auto-update 决策：初版用规则判断（completed -> done, failed -> blocked），不引入 LLM-assisted subgoal update。固定 loop activity 合同：`last_loop_tick` 在每次 `continueNode` 完成后写入 `task.metadata`，前端基于 `last_loop_tick` 与当前时间差值判断 loop 活跃度（active/stall/stale）。后续相关变更必须先判断是 goal progress 优先级调整、subgoal 自动迁移规则、还是 loop activity 活跃度判断逻辑变更。


- 2026-07-22: CCX + Pi + Harness + Advisor 四层集成的稳定产品决策已经固定并完成 P1/P2/P3 落地。固定口径是：Pi 不作为新的执行层，而是作为 harness 的 small-tier worker 通过 `ProviderCliWorkerExecutor` / `PiProtocol` 调度；Trae Advisor 不是人在环，而是“低能力模型向高能力模型请教思路”的模型间咨询，落在 `ControlNodeGraph` 的 advisory handoff 分支，`HandoffPacket.why_handoff` / `metadata.handoff_reason` 使用 `advisory_consult`；CCX 继续只负责 provider 网关层的 API 转换、渠道/Key/failover，不把这些逻辑复制进 harness；不引入新的 gRPC/WebSocket/event bus，Pi/Trae 均通过 CLI protocol 接入。P1 `PiProtocol`、P2 advisory handoff、P3 `TraeProtocol` 已分别由 `PiProtocolTest`、`AdvisoryHandoffTest`、`TraeProtocolTest` 覆盖。后续相关变更必须先判断是 provider protocol 接入、loop/advisory 语义、还是 CCX 网关配置问题，不要重新引入独立 AdvisorService 或第二套 provider 路由。
- 2026-07-21: Codex 多 provider 的协议差异（Responses API ↔ Chat Completions 转换、渠道优先级、Key 轮换、模型名混淆兜底）现在统一由本机 CCX 网关（`BenedictKing/ccx`，`127.0.0.1:4243`）收敛，不再由 harness 负责。harness 只消费规范化的 provider run metadata / status / events，把 provider 执行层视为归一化后端信号；codex-openai / xfyun / deepseek profile lane、codebuddy/deveco CLI 接入这类 provider 专项工作从主线优先级降级，只在接入新协议或读面诊断时推进。下一阶段 harness 聚焦四条线：Loop 主闭环（`goal -> plan -> execute -> judge -> decide`，HTTP `/continue` 超时不把 active 任务判成 `failed`）、Goal 目标合同（`goal / subgoals / subgoal_status / acceptance_criteria / progress_summary`）、上下交接文档（Resume / Handoff packet 最小字段集正式写进 `API_CONTRACTS.md` / `SPEC.md`）、UI 页面展示结果 / 返回与执行中状态判断（`active / running / waiting_human / failed / partial / done` 状态语义与页面展示）。新方向主入口为 `docs/LOOP_GOAL_HANDOFF_UI_FOCUS_PLAN.md`，并取代 `NEXT_5_ENGINEERING_PRIORITIES.md` / `CURRENT_CAPABILITY_GAP_ASSESSMENT.md` 的 P1–P5 排序口径。约束：不要把 CCX 的渠道编排、Key 轮换、模型名映射逻辑复制进 harness；harness 侧 provider lane 配置只保留“指向 CCX 网关 + 模型名”这一层。
- 2026-06-30: 专题入口的最小结构合同现在统一收成六段，不再把 `稳定基线` 只视为业务主题的附加层。固定口径是：每个 `docs/<topic>/README.md` 都必须继续保留 `命中信号 / 最小阅读顺序 / 稳定基线 / 当前主线文档 / 写回顺序 / 历史材料(使用规则)`，并在 `稳定基线` 段里显式说明哪些正式文档“今天仍然为真”。业务主题额外再保留子主题分流表与 `当前入口建议`；这层统一合同现已进入 `Run-DocsIndexAudit.ps1` 与 `DocsStructureContractTest`。
- 2026-06-30: 专题入口和 `PROGRESS.md` 的段落顺序现在也进入了稳定合同，不再只是文风偏好。固定口径是：专题 `README.md` 必须保持 `命中信号 -> 最小阅读顺序 -> 稳定基线 -> 当前主线文档 -> 写回顺序` 的核心顺序，已启用 `PROGRESS.md` 的专题则必须保持 `当前状态 -> 已完成 -> 活跃子线 -> 下一步 -> 风险`；这层顺序合同已进入 `Run-DocsIndexAudit.ps1` 与 `DocsStructureContractTest`，后续若有人打乱顺序，应视为结构回归而不是排版差异。
- 2026-07-06: 主题工作区里一旦启用了 `tasks/`、`runs/` 或 `archive/` 这类二级目录，就不再允许“只有目录，没有入口”。固定口径是：二级目录至少要有对应的 `README.md` 做主题级聚合入口，并由 `docs/<topic>/README.md` 显式说明其职责；当前第一条落地实例是 `evaluation/runs/README.md`，它负责聚合 dated execution evidence，但不替代 root-level dated 文档本体。后续若其他主题也启用 `runs/`、`tasks/` 或 `archive/`，都应沿用“先建二级入口 README，再谈物理迁移或更细分组”的顺序。
- 2026-07-06: `runs/README.md` 的稳定职责现在明确为“主题内 evidence 聚合入口”，而不是“搬迁后的新文档本体目录”。当前 `evaluation/runs/README.md` 与 `dialogue/runs/README.md` 都采用同一口径：聚合 root-level dated execution / acceptance / precheck 文档的主题内入口，并要求仍然有效的结论优先回收到主题 README、PROGRESS、runbook 或稳定基线文档。后续若其他主题启用 `runs/`，默认也应先复用这条轻量聚合模式。
- 2026-07-06: `continuity/runs/README.md` 现在也采用同一条稳定口径：聚合 packet / control-route / multi-round / closure 相关的 root-level dated execution record，但不替代 `LIVE_FLOW_RUNBOOK.md`、`ARCHITECTURE.md`、`SPEC.md`、`API_CONTRACTS.md` 这些 continuity 主题的正式主线与稳定基线。也就是说，runs 入口负责“按主题找证据”，基线/README/PROGRESS 仍负责“今天到底以什么为准”。
- 2026-06-30: `AGENTS.md` 的稳定角色现在固定为“Agent 开工入口”，不再兼任项目百科。固定口径是：`AGENTS.md` 只保留开工顺序、连续性规则、文档边界、当前工作区现状、开工红线、项目事实入口、必须先知道的风险和改动约束；项目概述、技术栈、源码树、HTTP 端点速查这类稳定事实，优先沉淀到 `docs/ARCHITECTURE.md`、`docs/API_CONTRACTS.md`、`docs/SPEC.md`、`docs/TROUBLESHOOT.md`、`docs/WEB_CONSOLE.md` 等正式文档，而不是长期堆回根入口。后续如果再想扩写 `AGENTS.md`，应先判断是在补 Agent 约束，还是其实应该更新正式基线文档。 
- 2026-06-30: `README.md` 与 `STARTUP_GUIDE.md` 现在也纳入根入口稳定合同，不再只是“文案上顺手做了分层”。固定口径是：`README.md` 必须保留公开入口导航，至少把读者送到 `docs/README.md`、`docs/meta/README.md`、`docs/DOCS_GOVERNANCE.md`、`WAKE.md`、`AGENTS.md`、`STATE.md`、`DECISIONS.md`；`STARTUP_GUIDE.md` 必须保留“本文边界”，并把非启动类任务导回 `docs/README.md`、`docs/dialogue/README.md`、`docs/provider/README.md`、`WAKE.md`、`AGENTS.md`、`STATE.md`、`DECISIONS.md`。这层合同现已同时进入 `Run-DocsIndexAudit.ps1`、`DocsStructureContractTest` 与 `DocsIndexAuditScriptTest`。 
- 2026-06-30: `release/` 继续保持 `README-only` 现在视为显式稳定口径，不再只是“暂时还没升级”。固定理由是：当前 release 真相已经集中在 checklist、scope proposal、execution guide 与最新 precheck 这组稳定基线里，剩余多数 release 文档属于 dated 历史 dry-run / stage-preview / commit-plan 证据，而不是持续高频推进的活跃线程。只有当新一轮 release 周期开始，并出现连续短进度写回、多份新 dated precheck/dry-run 证据集中追踪，或并行 release 子任务显著增多时，才允许把 `release/` 升级为 `README.md + PROGRESS.md` 的主题工作区，并继续视需要增加 `runs/`、`tasks/`、`archive/`。 
- 2026-06-30: 文档治理的审计入口现在采用“三层回归”稳定口径，不再让 `Run-DocsIndexAudit.ps1` 只靠人工手跑。固定分工是：`Run-DocsIndexAudit.ps1` 负责可执行的差集/结构审计；`DocsStructureContractTest` 锁住 docs 目录与根入口的结构不变量；`DocsIndexAuditScriptTest` 负责直接执行该脚本并校验 `summary.passed / violation_count / wake|agents workspace row coverage / 根入口阅读链布尔值`。后续如果脚本语法坏了、summary 字段漂移了，focused Maven 回归就应该先失败。 
- 2026-06-30: 根入口文档现在也纳入文档结构回归，不能再只靠人工同步。稳定口径是：`WAKE.md` 与 `AGENTS.md` 必须显式列出每个 `docs/<topic>/` 当前是 `README.md + PROGRESS.md` 还是 `README-only`，并固定两条默认阅读链：已升级主题走 `README.md -> PROGRESS.md -> 当前主线文档`，`README-only` 主题走 `README.md -> docs/` 根目录主线文档。该口径现已同时进入 `Run-DocsIndexAudit.ps1` 与 `DocsStructureContractTest`；后续若再升级 `release/` 或新增专题，根入口未同步应直接视为结构回归。 
- 2026-06-30: 文档治理本身现在采用独立专题入口，不再继续只挂在 `docs/README.md` 的说明文字里。固定口径是：文档结构整理、索引审计、命名合同、专题工作区升级这类任务先进入 `docs/meta/README.md`；稳定结构合同统一写进 `docs/DOCS_GOVERNANCE.md`；活跃文档治理进度写进 `docs/meta/PROGRESS.md`。这也是当前唯一已经启用 `PROGRESS.md` 的专题，其他业务主题继续保持 README-only，直到它们各自真的进入持续高频续写。 
- 2026-06-30: 主题工作区升级继续采用“按单一主题逐个放行”的稳定口径，但不再限定只有 `meta/` 能启用 `PROGRESS.md`。当前已固定的工作区状态是：`meta/` 承接文档治理，`dialogue/` 承接 `/dialogue/`、`/console/`、acceptance、operator 读面的持续推进；两者都采用 `README.md -> PROGRESS.md -> 当前主线文档` 的阅读顺序。其余 `continuity / provider / evaluation / release` 仍保持 `README-only`，直到各自真的进入持续高频续写。 
- 2026-06-30: `provider/` 现在也进入了“单主题持续高频推进，允许升级工作区”的稳定范围。固定状态已更新为：`meta/` 承接文档治理，`dialogue/` 承接 UI/acceptance，`provider/` 承接路由、profile、CLI 接入与恢复策略；这三个主题都采用 `README.md -> PROGRESS.md -> 当前主线文档` 的阅读顺序。其余 `continuity / evaluation / release` 仍保持 `README-only`，直到各自真的进入持续高频续写。 
- 2026-06-30: `continuity/` 现在也进入了“单主题持续高频推进，允许升级工作区”的稳定范围。固定状态已更新为：`meta/` 承接文档治理，`dialogue/` 承接 UI/acceptance，`provider/` 承接路由与接入，`continuity/` 承接控制面主链、packet、live flow 与多轮续跑；这四个主题都采用 `README.md -> PROGRESS.md -> 当前主线文档` 的阅读顺序。其余 `evaluation / release` 仍保持 `README-only`，直到各自真的进入持续高频续写。 
- 2026-06-30: `evaluation/` 现在也进入了“单主题持续高频推进，允许升级工作区”的稳定范围。固定状态已更新为：`meta/` 承接文档治理，`dialogue/` 承接 UI/acceptance，`provider/` 承接路由与接入，`continuity/` 承接控制面主链，`evaluation/` 承接项目评估、优先级、任务包与执行证据；这五个主题都采用 `README.md -> PROGRESS.md -> 当前主线文档` 的阅读顺序。`release/` 仍保持 `README-only`，直到它真的进入持续高频续写。 
- 2026-06-30: 根入口继续采用“按角色分层、各文档只回答自己的问题”的稳定口径，不让 `README`、`STARTUP_GUIDE`、`docs/README` 再互相混职。固定分工是：`README.md` 负责公开概览与快速开始，`STARTUP_GUIDE.md` 只负责构建/启动/运行验证/启动期排障，`docs/README.md` 负责开发/排查/文档整理分流，`WAKE.md`/`AGENTS.md` 负责 Agent 开工与协作规则，`STATE.md`/`DECISIONS.md` 负责连续性写回。后续如果再优化文档结构，优先补这种入口分层和跳转链，而不是先新增目录或物理迁移历史文件。 
- 2026-06-30: root-level 正式文档采用“总索引可达 + 专题入口归属”双重口径，不接受只挂在 `docs/README.md` 的游离材料。固定规则是：`docs/` 根目录每一份正式 Markdown 都必须至少被一个 `docs/<topic>/README.md` 引用；`docs/README.md` 只负责总分流，不代替专题入口承担具体文档归属。该口径现已同时进入 `Run-DocsIndexAudit.ps1` 与 `DocsStructureContractTest`，后续如果出现“只有总索引知道、主题入口不知道”的 root doc，应视为结构回归。 
- 2026-06-30: root-level dated 文档命名采用“核心合同 + 显式历史例外”的稳定口径，不再靠人工记忆判断哪些旧命名还能保留。固定规则是：新 dated 文档默认只能使用 `*_EXECUTION_RECORD_YYYY-MM-DD.md`、`*_ACCEPTANCE_RECORD_YYYY-MM-DD.md`、`*_PRECHECK_YYYY-MM-DD.md`；现有 release `*_DRY_RUN_*`、`*_STAGE_PREVIEW_*`、`*_COMMIT_DRY_RUN_*` 以及少量已经在专题入口里明确降级为历史材料的旧 dated 设计/快照文档，作为 grandfathered historical exceptions 保留，但不再扩散新实例。该口径现已同时进入 `Run-DocsIndexAudit.ps1` 与 `DocsStructureContractTest`。 
- 2026-06-30: docs 结构合同采用“PowerShell 审计 + JUnit focused regression”双层口径，不再让结构规则只依赖单一脚本入口。固定分工是：`scripts/Run-DocsIndexAudit.ps1` 负责仓库内可执行的差集/合同审计与 Markdown 快照输出；`DocsStructureContractTest` 负责把最核心的结构不变量锁进 Maven 回归，包括专题 README 必备段落、root-level `docs/*.md` 可达性、`docs/README.md` 的专题入口引用以及 README-only 现状表同步。这样后续即使没有手跑脚本，focused test 也会先暴露文档入口结构漂移。 
- 2026-06-30: docs 审计脚本默认按 UTF-8 无 BOM 处理仓库 Markdown，这条编码约束现在视为稳定实现规则，不再依赖执行环境的默认代码页。固定口径是：凡是用脚本读取 `docs/*.md`、`docs/<topic>/README.md`、`STATE.md`、`DECISIONS.md` 这类中文文档时，都应显式指定 UTF-8 读取；否则在 Windows PowerShell 5.1 下会把专题标题和工作区现状表读坏，导致结构审计出现假阳性。`scripts/Run-DocsIndexAudit.ps1` 现已按这一口径收口。 
- 2026-06-30: docs 索引审计的稳定范围再扩一层，不再只检查“有没有入口引用”。固定口径是：`Run-DocsIndexAudit.ps1` 除了验证专题入口覆盖、root-level `docs/*.md` 不得 orphan、主题目录顶层轻量工作区合同外，还必须验证每个 `docs/<topic>/README.md` 都具备 `命中信号 / 最小阅读顺序 / 当前主线文档 / 写回顺序 / 历史材料(使用规则)` 这组最小入口段落，以及 `docs/README.md` 的“当前专题工作区现状”表与真实目录状态保持同步。这样后续再做文档结构演进时，不只知道“文档被挂上了”，也能确认“入口本身仍符合当前专题合同”。 
- 2026-06-30: docs 索引审计不再只是口头流程，固定提供仓库内可执行入口。稳定口径是：完成专题入口或总索引调整后，优先运行 `powershell -ExecutionPolicy Bypass -File .\scripts\Run-DocsIndexAudit.ps1`；需要保留快照时再加 `-WriteMarkdown -MarkdownPath .tmp\docs-index-audit.md`。该脚本当前验证三类约束：`docs/README.md` 必须引用所有专题入口、root-level `docs/*.md` 不得 orphan、专题目录顶层只允许 `README.md / PROGRESS.md / tasks / runs / archive` 这组轻量工作区合同。这样后续做文档结构演进时，差集审计本身也成为可重复执行的工程资产。 
- 2026-06-30: 文档结构优化任务本身采用固定的四步顺序，不再让这套规则只停留在 `docs/README.md`。稳定口径是：先用 `docs/README.md` 判断主题，再在对应 `docs/<topic>/README.md` 收入口和阅读顺序，只有入口已经收实后才考虑动历史文件位置；完成一轮入口重构后，必须补一次 root-level `docs/*.md` 差集审计，并把结果至少写回 `STATE.md`。这样后续再参考 `articleeditor` 做结构优化时，重点始终是“入口闭环 + 审计闭环”，不是先扩目录。 
- 2026-06-30: free-first/manual-window 的执行摘要面也采用“与 route/read surface 共用同一组稳定字段”的口径，不再让 `/agent_run` 自己退化成半截 route trace。固定做法是：`AgentRunService.providerSelection(...)` 与 `recordWorkerRun(...)` 都直接复用 `WorkerRouter.RouteResult` 派生结果，把 `free_first_routing`、`cost_route_stage`、`manual_window_required`、`recommended_manual_provider`、`manual_window_candidates`、`manual_followup_instruction` 一并投影到 `live_flow.provider_selection.metadata` 与 `/api/v1/tasks/{id}/agent_run.metadata`。执行摘要面不单独拼 follow-up 文案，避免 route/read surface、task metadata 和 agent run 三处再度漂移。 
- 2026-06-30: 当前文档工作区状态采用“统一显式写明、按真实活跃度逐个升级”的稳定口径，不再只写升级规则让读者自己猜现状。固定事实是：`meta / dialogue / provider / continuity / evaluation` 已启用 `PROGRESS.md`，默认阅读链为“`docs/<topic>/README.md` -> `docs/<topic>/PROGRESS.md` -> 当前主线文档”；`release` 仍只有 `README.md`，默认阅读链仍是“`docs/<topic>/README.md` -> `docs/` 根目录主线文档或 dated record”。只有某一主题真的进入持续高频续写，才允许把该主题升级成轻量工作区；在那之前，不预创建空目录层，也不做批量历史迁移。 
- 2026-06-30: free-first/manual-window 的 `manual_followup_instruction` 不再只作为前端复用的 task metadata 文案，而是正式提升为稳定后端读面字段。固定口径是：`WorkerRouter.RouteResult` 统一派生这条文案，raw `/select_worker`、`/live_flow.route_preview`、`runtime_cognition_surface.route`、`/provider_selection.metadata` 都直接透出同名字段；`ControlNodeGraph` 进入 `human_gate` 时也复用同一来源，避免 route/read surface 与 task metadata 文案漂移。Dialogue action note 仍可继续直接消费 task metadata，但那只是同一稳定文案的前端入口，不再是唯一来源。 
- 2026-06-30: manual-window 的 follow-up 提示继续采用“前端复用既有 task metadata，不扩 route preview 合同”的稳定口径。固定做法是：`/dialogue/` 的 task action note 直接消费 `manual_window_required`、`recommended_manual_provider`、`manual_window_candidates`、`manual_followup_instruction`；route box / console summary 继续消费 route metadata 做 paid fallback / manual window blocker 解释。也就是说，“为什么停下”与“停下后怎么做”分别复用 route metadata 与 task metadata，不为了单条 follow-up 文案新增 HTTP 字段。 
- 2026-06-30: 文档结构的稳定口径进一步收紧为“专题轻量工作区渐进升级”。固定规则是：所有主题默认只有 `docs/<topic>/README.md`；只有某一主题连续高频推进时，才按顺序增加 `PROGRESS.md`、`tasks/`、`runs/`、`archive/`。阅读顺序固定为 `README.md -> PROGRESS.md -> 当前 plan/runbook/record -> runs/archive`，其中 `PROGRESS.md` 只承载该主题活跃进展，不替代 `STATE.md` / `DECISIONS.md`。这样既吸收了 `articleeditor` 的工作区式连续性习惯，又避免把本仓库升级成全局 `memory/`、`state/` 树。 
- 2026-06-30: free-first provider 路由的前端读面采用“复用既有 route metadata，在 Dialogue/Console 统一做人话解释”的稳定口径，不额外引入新的 HTTP 合同字段。固定做法是：`/dialogue/` 的 route box 和 `/console/` 的 operator summary 都应直接消费 `manual_window_required`、`recommended_manual_provider`、`manual_window_candidates`、`cost_route_stage`、`free_candidate_workers`、`paid_candidate_workers`、`fallback_reason`；其中 `manual_window_required` 要翻译成“自动链路已停下，建议切到某个手动窗口继续”，`cost_route_stage=paid_auto` 要翻译成“免费自动链路不可用，当前已回退到付费 provider”。这样 free-first 不再只是 raw route metadata，而是稳定进入用户/操作者首屏读面。 
- 2026-06-30: `release` 主题的稳定入口口径改成“先按子主题分流，再进入具体文档”，不再把 checklist、scope、execution guide、precheck、dry-run、stage preview 和 commit 计划视为同层主线。当前固定的五类子线是：`release gate / 当前是否可公开`、`首发范围边界 / 对外叙事`、`replay / stage / commit 执行链`、`最新 dated precheck 证据`、`历史 dry-run / stage-preview / commit-plan 工作日志`。稳定基线优先落在 `GITHUB_RELEASE_CHECKLIST.md`、`GITHUB_RELEASE_SCOPE_PROPOSAL.md`、`GITHUB_FIRST_RELEASE_EXECUTION_GUIDE.md`、`GITHUB_FIRST_RELEASE_PRECHECK_2026-06-02.md`、`GITHUB_SUBMISSION_AND_EVOLUTION_PLAN.md`；`GITHUB_FIRST_RELEASE_DRY_RUN_2026-05-11.md`、`GITHUB_FIRST_RELEASE_STAGE_PREVIEW_*`、`GITHUB_FIRST_RELEASE_COMMIT_DRY_RUN_*`、`GITHUB_FIRST_RELEASE_INDEX_AUDIT_2026-05-11.md` 这批 dated 工作日志不再默认作为 release 主题第一入口。 
- 2026-06-30: 专题入口收口后，稳定流程增加一轮 root-level `docs/*.md` 差集审计，不再只凭人工目测判断“应该都挂到了”。固定口径是：完成某个专题或总索引重构后，应重新核对 `docs/` 根目录正式 Markdown 是否都能从 `docs/README.md` 或某个 `docs/<topic>/README.md` 被追到；若某份文档更适合某个专题入口，就优先补专题入口引用，而不是只让它停留在总索引。当前这轮审计结果已经为 `ORPHANS=<none>`，因此下一阶段的重点不再是补漏挂，而是继续压实各专题内部的主线/历史材料边界。 
- 2026-06-30: `evaluation` 主题的稳定入口口径改成“先按子主题分流，再进入具体文档”，不再把优先级、评测、对标、产品化、多轮任务链和 dated execution record 视为同层主线。当前固定的五类子线是：`能力差距 / 项目评价 / 工程优先级`、`目标导向评测 / 评估场景`、`对标 / 借鉴 / 产品化`、`多轮任务包 / 测试驱动 / 执行链`、`dated execution record / 专项历史设计`。稳定基线优先落在 `CURRENT_CAPABILITY_GAP_ASSESSMENT.md`、`PROJECT_EVALUATION_AND_NEXT_PLAN.md`、`NEXT_5_ENGINEERING_PRIORITIES.md`、`PHASE2_ROADMAP.md`；`TASK_3809507EDBBE4231_LONG_TASK_SUCCESS_RATE_DESIGN_2026-05-15.md` 这类早期专项设计稿不再与真实 dated execution record 处于同层默认入口，只有在回看当时的长任务失败样本和设计思路时才应优先进入。 
- 2026-06-30: `continuity` 主题的稳定入口口径改成“先按子主题分流，再进入具体文档”，不再把所有控制面、goal、runtime、多轮验证文档视为同层主线。当前固定的五类子线是：`稳定控制面基线与合同`、`continuity / packet / runtime-memory 主线`、`goal loop / agent action / bounded autonomy 设计线`、`live validation / 多轮执行 / runbook 证据线`、`provider partial-result / 本地 agent 重设计` 这类跨主题诊断线。稳定基线优先落在 `ARCHITECTURE.md`、`SPEC.md`、`API_CONTRACTS.md`、`LIVE_FLOW_RUNBOOK.md`、`TROUBLESHOOT.md`、`HARNESS_CHANGE_CONTRACT.md`；`HARNESS_EVOLUTION.md`、`ORCHESTRATION_MVP_PLAN.md`、`HOW_AGENT_CLOUD_HARNESS_FITS_MEMORY_ARCHITECTURES.md`、`GOAL_RUNTIME_LANDING_DIFF_2026-05-11.md`、`HARDNESS_PHASE1_*`、`EXECUTION_ENVELOPE_PHASE1_STATUS.md`、`LIVE_FLOW_TRACE.md` 不再默认作为 continuity 主题第一入口；`HARNESS_REDESIGN_FOR_LOCAL_AGENTS.md` 只有在任务明确落到 partial result、artifact 展示断链或本地 agent 重设计时才应优先进入。 
- 2026-06-30: `dialogue` 主题的稳定入口口径改成“先按子主题分流，再进入具体文档”，不再把所有 `DIALOGUE_*` 文档视为同层主线。当前固定的五类子线是：`chat-first 产品壳层`、`message / facade / continuity contract`、`浏览器验证 / acceptance / release gate`、`console / operator 诊断面`、`布局与渲染实验`。稳定基线优先落在 `WEB_CONSOLE.md`、`DIALOGUE_UI_VALIDATION_RUNBOOK.md`、`DIALOGUE_CHAT_FACADE_ACCEPTANCE_RUNBOOK.md`、`TROUBLESHOOT.md`；`DIALOGUE_DYNAMIC_RENDER_V2.md`、`DIALOGUE_ELASTIC_CARD_PLAN.md`、`DIALOGUE_PRETEXT_DYNAMIC_LAYOUT_PLAN.md` 这类布局实验文档，以及 `HARNESS_REDESIGN_FOR_LOCAL_AGENTS.md` 这类跨主题诊断稿，不再默认作为 dialogue 主题第一入口。 
- 2026-06-30: 当某个专题内部已经分化出多条真实子线时，专题入口必须先做子主题分流，不能继续把所有文档平铺成“当前主线”。当前固定口径已经先在 `provider` 主题落地：先区分 `provider 总体接入/API contract`、`全局路由与恢复`、`codex family 多 profile lane`、`具体 CLI 参数与 parser 接入`、`工具试点与观测面`，再分别给出第一入口。像 `AGENT_PROVIDER_CODE_SKELETON_PLAN.md` 这类 phase-1 scaffold、`LOCAL_DOC_WORKER_PILOT.md` / `REASONIX_AGENT_SUPPORT_AND_IMPROVEMENT_PLAN.md` 这类专项试点文档，不再默认充当 provider 主题第一入口。 
- 2026-06-30: 文档类型采用固定合同，不再把 `plan / design / runbook / execution record / acceptance / precheck` 当成可互换叫法。稳定口径是：待办拆解与推进步骤用 `*_PLAN.md`；方案边界、字段合同、设计取舍用 `*_TECHNICAL_DESIGN.md` 或既有 `*_DESIGN.md`；可 replay 的命令链、操作步骤、排障步骤用 `*_RUNBOOK.md`；一轮真实执行证据用 `*_EXECUTION_RECORD_YYYY-MM-DD.md`；一轮真实验收证据用 `*_ACCEPTANCE_RECORD_YYYY-MM-DD.md`；release 预检快照用 `*_PRECHECK_YYYY-MM-DD.md`；模板骨架用 `*_TEMPLATE.md`；跨阶段推进顺序用 `*_ROADMAP.md`。除既有 release 历史材料外，新 dated 文档统一使用绝对日期后缀 `YYYY-MM-DD`，不再继续扩散新的平行命名模式。 
- 2026-06-30: 专题入口的稳定口径继续收紧到“覆盖完整 docs 根目录”，不接受 root-level orphan docs 长期裸放。稳定规则是：只要 `docs/` 新增或保留一份正式 Markdown，它就必须能从 `docs/README.md` 或某个 `docs/<topic>/README.md` 被追到；如果主题已经明确，就优先挂到专题入口，而不是只留在总索引或完全不挂。当前额外固定的分类口径包括：hardness / goal runtime diff -> `continuity`，本地 CLI 兼容性与编码 -> `provider`，页面 release gate -> `dialogue`，benchmark / productization -> `evaluation`，commit/stage/fileset -> `release`。这条规则的目的不是追求目录好看，而是让“按任务开工”真正落到入口可达，而不是继续从 root-level 长名单里猜。 
- 2026-06-30: SSE 读面测试采用“等待真实事件到达”而不是 `sleep` 推测时序的稳定口径。对 `provider_run_file.snapshot/update` 这类窗口流回归，测试必须等到首个 snapshot 和预期内容真正到达后，再触发文件变化或后续断言；不能用固定等待时间去假设网络/线程调度顺序，否则测出来的是时序噪音，不是合同回归。对外口径也保持不变：`provider_run_file` SSE 验证的是受控尾部窗口读面 update 语义，不是 token 级 stdout streaming。 
- 2026-06-30: 当前 Windows/JDK 21 工作站上的 Surefire manifest classpath 噪音视为仓库级测试基础设施问题，固定在 `pom.xml` 收口，不再要求操作员手工加环境变量。稳定口径是：`maven-surefire-plugin.argLine` 必须包含 `-Djdk.net.URLClassPath.disableClassPathURLCheck=true`，用来避开 `TaskRuntimeContextBuilder` / `schema.sql` 这类类路径假失败；只有在该参数仍生效的前提下出现异常，才继续怀疑真实代码或资源回归。 
- 2026-06-30: Codex profile route 的诊断读面采用“三个显式字段统一透传”的稳定口径，不再只靠 `selected_worker` 间接推断。固定字段为 `selected_provider_profile`、`preferred_provider_profile`、`workflow_stage`；它们应同时出现在 `WorkerRouter.RouteResult`、`RouteDiagnostic`、`/select_worker`、`/provider_selection`、`/live_flow.route_preview`、`runtime_cognition_surface.route` 与 runtime fact export 中。`task_pinned` 成功链路也不能绕过 codex family 内部的二层 profile 路由，否则 operator 会在读面看到“worker 被 pin 到 codex”，但看不出实际落到哪条 profile lane。 
- 2026-06-30: 文档入口的稳定口径再收紧一层，采用“总索引 -> 任务分流 -> 专题入口 -> 具体文档”的固定链路。`docs/README.md` 只回答“当前任务该进哪个主题、文档分层是什么、新文档该落哪里”；每个 `docs/<topic>/README.md` 固定回答“命中信号、最小阅读顺序、当前主线、写回顺序、历史材料”。新产生的 `plan / runbook / execution record / acceptance record` 必须能被某个专题入口追到；历史材料允许保留，但默认不作为第一入口。当前仍坚持轻量结构，不批量迁移 root-level 正式文档，也不引入 `memory/`、`state/` 目录树。 
- 2026-06-30: Codex profile 执行链与路由第一版稳定口径已落地。数据合同采用 `ProviderProfileConfig` record，合并优先级固定为 task > worker > provider default；`CodexExecutionPlan` 新增 `providerProfileId / modelProvider / cliProfile / configOverrides` 四字段；`thread/start` 的 `modelProvider / profile / config` 不再写 null，改为从 resolved profile passthrough；CLI 参数形态为 `-c model_provider=xxx -m xxx -p xxx`，与本机 codex-cli 0.142.0 实证对齐。Codex profile 路由采用"进入 codex family 后的二层路由"稳定口径：`preferred_provider_profile` 显式优先 > `workflow_stage` 阶段驱动（`design/verify -> codex-openai`、`implement -> codex-xfyun`）> 无信号时保守沿用旧 `codex` worker lane。Profile 失败分类新增 `quota_exhausted / auth_blocked / backend_unavailable / profile_misconfigured / dispatch_preflight_failed` 五类，与现有 provider 级 `ProviderFailureClassifier` 并存不互相覆盖。
- 2026-06-30: 文档结构继续参考 `articleeditor` 的连续性思路，但这次正式把“专题入口”固定成仓库标准层。稳定口径是：根目录入口仍由 `WAKE.md` / `AGENTS.md` / `STATE.md` / `DECISIONS.md` 承担，`docs/README.md` 只做总索引与写回规则，具体主题先走 `docs/<topic>/README.md` 这样的专题入口；当前专题入口至少包括 `continuity / provider / dialogue / evaluation / release` 五类。现阶段这些目录只承担索引作用，不迁移 root-level 正式文档；只有某一主题持续高频膨胀时，才允许在该目录内继续增加 `PROGRESS.md`、`tasks/`、`runs/`、`archive/`。 
- 2026-06-30: `codex` 对接不同 API / 账户通道时，稳定口径采用“一个 provider，多条 profile lane”，而不是拆成多个伪 provider。固定边界是：`providerId=codex` 保持不变；后续若要暴露 `OpenAI / xfyun / deepseek` 三条 codex 通道，应引入命名 `provider_profile_id`，并允许 `codex-openai / codex-xfyun / codex-deepseek` 作为多个 worker lane 出现在控制面。profile 选择属于“进入 codex family 之后的第二层路由”，不能覆盖现有 `free_first` 顶层 provider 路由；也就是说，系统应先决定是否进入 `codex` family，再在 family 内部按 `design / implement / verify` 阶段或显式 pin 选择 `openai / xfyun / deepseek`。同时，`model_provider` 这类 CLI override 作为 passthrough 处理，不在 harness 侧擅自改写大小写或语义。
- 2026-06-18: 免费优先 provider 路由采用“cost-stage 分层 + 现有组内排序”而不是仅靠 `selection_priority` 调权重。稳定口径是：`deveco` 归 `free_auto`，`codebuddy` 归 `free_auto_guarded`，`codex / reasonix` 归 `paid_auto`，`trae / zcode` 归 `manual_window`；`manual_window` provider 只做推荐，不进入自动 route / auto-recovery / auto-handoff 候选。自动链无法继续时，系统进入 `waiting_human / human_gate`，并通过 metadata 返回 `manual_window_required`、`recommended_manual_provider`、`manual_window_candidates`、`manual_followup_instruction`。
- 2026-06-17: 历史文档治理采用“先提炼、再降级、后归档”的顺序，而不是先搬文件。仓外 Obsidian / 学习沉淀文档可以作为启发来源，但不能直接充当仓库事实；只有写回到本仓库并完成代码/测试对齐后，才进入当前主线文档链。文档推进也继续采用“问题/计划 -> runbook -> focused test/probe -> execution record -> STATE/DECISIONS”的闭环，避免只写方案不落验证。
- 2026-06-17: 文档结构不照搬 `articleeditor-tmp/docs/multimedia-agent-skill-progress/` 的整套多级目录，而是只吸收“专题工作区闭环”这一点：默认继续采用 `docs/README.md` 驱动的索引优先、原位维护；只有当单一主题长期高频续写且同时承载方案、runbook、记录、待办、归档时，才允许局部新增 `docs/<topic>/README.md + PROGRESS.md + tasks/ + runs/ + archive/` 形式的专题工作区。
- 2026-06-15: 文档结构继续沿用 `articleeditor` 的连续性思路，但保持本仓库的轻量写回面：`WAKE.md` 负责开工顺序，`AGENTS.md` 负责协作约定与目录边界，`docs/README.md` 负责 `docs/` 索引和落点，`STATE.md` / `DECISIONS.md` 只记录跨主题短状态和稳定取舍。
- 2026-06-14: `WorkerRegistry` 的 worker priority config 保持“生产默认自动发现、测试默认隔离”策略：运行时继续按默认路径搜索 `workers.yaml/yml`，但测试通过 Surefire 默认关闭 `agentcloud.worker.priority.config.enabled`，只有显式传入 config path 的用例才启用覆盖，以避免本地操作员配置污染回归结果。
- 2026-06-14: 文档整理继续采用“索引优先、原位维护”策略：根目录 `README.md` 只保留仓库级入口，`docs/README.md` 维护主题分类与写回地图；除非已经完成引用审计，否则不批量重命名或迁移历史文档。
- 2026-06-09: 参考 `articleeditor` 的连续性习惯，但本仓库采用更轻量的写回面：`WAKE.md` 负责开工顺序，`STATE.md` 记录跨主题最近进度，`DECISIONS.md` 记录稳定取舍，主题内方案/验收继续沉淀到最贴近的 `docs/*.md`，不引入额外的 `memory/`、`state/` 目录树。
- 2026-05-05: Keep the trace/flow API shape stable, and move aggregation behind `RuntimeFactSet` first.
- 2026-05-05: Prefer incremental internal refactors over replacing `JudgmentTraceView` / `TaskLiveFlowView` all at once.
- 2026-05-05: Continue converging internal progress/message projection onto `RuntimeFactSet` before introducing broader external contract changes.
- 2026-05-05: Promote `RuntimeFactSetAssembler` to a first-class `TaskService` dependency before expanding the next layer of runtime contracts.
- 2026-07-06: `runs/README.md` 的入口合同现在再收紧一层，不再只要求“主题 README 能提到 runs/”。固定口径是：只要某个主题启用了 `runs/`，对应 `docs/<topic>/README.md` 必须显式说明 `runs/README.md` 的职责与入口位置，`docs/README.md` 的“当前专题工作区现状”默认阅读路径也必须把 `runs/README.md` 暴露出来；换句话说，`runs/` 不能只是目录存在、README 存在，还必须在主题入口和总索引两层都真实可达。当前 `continuity / provider / dialogue / evaluation` 都已采用这条口径。
- 2026-07-06: `runs/README.md` 的入口合同现在还包括默认阅读链字面值本身。固定口径是：对已启用 `runs/` 的主题，专题 `README.md` 的默认阅读顺序必须显式写成 `README.md -> PROGRESS.md -> 当前子线文档 -> runs/README.md`，不能只写成目录级的 `runs/`；否则读者虽然知道有个 runs 目录，但默认链路并没有真正把他送到 evidence 入口文件。
- 2026-07-06: `runs/README.md` 现在也进入了稳定最小结构合同，不再只是“有个 README 就行”。固定口径是：已启用的 runs 入口至少要继续保留 `## 命中信号`、`## 最小阅读顺序`、`## 当前分组`、`## 使用规则` 四段，并显式说明它承担的是主题级 evidence 聚合入口，而不是 dated 文档搬迁后的新本体目录。
- 2026-07-06: `runs/README.md` 的角色说明现在也进入稳定字面合同，不再只要求读者能“推断出它是入口”。固定口径是：只要某个主题启用了 `runs/`，其 `runs/README.md` 就必须直接写明自己是该主题的 evidence 聚合入口；如果只有 dated 分组和链接、却没有这层角色说明，应视为结构回归。
