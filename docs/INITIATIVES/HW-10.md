# HW-10 - Jev x Judgment 前置过滤

> **状态**: 候选 (candidate)
> **创建**: 2026-09-21
> **维护者**: yangpeng@sobey.com

## 基本信息

| 字段 | 值 |
|---|---|
| 编号 | HW-10 |
| 类别 | harness 借鉴 |
| 优先级 | P1 |
| Phase | Phase 3 |
| 预估工时 | 1 天 |
| 关联文档 | ../JEV_CONTEXT_SCORING_PLAN.md §1.1a |
| 关联 Bitable 项目方向 | recvvNTY6alo4f |

## 状态机

```
候选 (candidate) -- 维护者签字 --> 立项中 -- 开始实施 --> 实施中 -- PR --> 验收中 -- 通过 --> 已关闭
                            |
                            `-- 撤回 --> 已撤销
```

**当前**: 候选 (合同 test + service 代码已落地)

## 立项门槛 (gating thresholds)

1. FEAT-03 已签字
2. Jev 跳过 PromptBasedJudgmentService 直接调 LLM
3. 跳过时 runtime_facts 标注 source=jev_skip

## 当前状态

2026-09-22：JevPrefilteredJudgmentService.java decorator 已实现（threshold 2+3 代码落地），JevJudgmentPrefilterTest 4/4 PASS（threshold 1 FEAT-03 已签字；skip LLM + source=jev_act/skipped_by_jev 均覆盖）。

## blocker

(无)

## sign-off checklist

- [ ] 维护者确认立项门槛全部满足
- [ ] 凭据纪律: key 仍按 ~/.openclaw/secrets/ 凭据约定存放
- [ ] CONTRIBUTING.md 该编号状态从候选切换到进行中
- [ ] Bitable 项目方向表 recvvNTY6alo4f 状态同步
- [ ] 实施 PR 链接记录到本 card

## 维护约定

- 状态切换改本文件的 当前 字段 + 文末当前状态小节
- INDEX.md 通过本文件路径聚合
- 关联 Bitable 通过软引用 (不建双向 schema 依赖)

## 参考

- 关联文档: ../JEV_CONTEXT_SCORING_PLAN.md §1.1a
- 设计依据: docs/JEV_OPENEYES_INITIATIVE_TABLE_DESIGN.md
- 本地落仓决策依据: 2026-09-21 Codex session (落仓优于新建 Bitable 表, 等 schema 稳定后迁)

## 14. 签字 + 迁入 src/ + 全仓库共存(2026-09-21)

**签字**:维护者(Codex session 2026-09-21)签字 HW-10。
理由:
- HW-10 依赖 FEAT-03 已签字(Jev-Graph-B path B prototype 4/4 测试绿)
- 不需要改 PromptBasedJudgmentService (有 5 个简化 constructor + 1 个 11-字段 primary constructor),JevPrefilter 是独立 contract test
- 中途纠错 2 处: (1) 用错 constructor (9 字段 vs 11 字段) → 改用 9-字段简化版; (2) Map.of() 强转 NPE → 用 Boolean.TRUE.equals 兜底

**迁入路径**:
- `src/test/java/com/agentcloud/scorer/JevJudgmentPrefilterTest.java` (5588 B, 4 个 JUnit 5 测试)
- 跑仓库自带 focused test: `.\scripts\Test-WithJava21.ps1 -Dtest=JevJudgmentPrefilterTest`
- 实测: `Tests run: 4, Failures: 0, Errors: 0 -- BUILD SUCCESS`

**全仓库共存验证**:
- 排除 DocsIndexAuditScriptTest 后跑 96 个 suite / 748 tests
- Failed suites 与 Jev-Graph-B + HW-09 跑通时**完全相同** (8 个 pre-existing)
- HW-10 不引入任何新 failure

**4 个 JUnit 测试**:
1. `obviousYesSkipsLLM`: state.obvious_yes=true → fakeJev=0.95 → routeJev="act" → callLlm=false, source="jev_act"
2. `obviousNoSkipsLLM`: state.obvious_no=true → fakeJev=0.05 → routeJev="skip_llm" → callLlm=false, source="skipped_by_jev"
3. `ambiguousFallsThrough`: state 空 → fakeJev=0.50 → routeJev="flag_for_review" → callLlm=true, source="fell_through_to_llm"
4. `jevScoreContract`: Jev noul probability ∈ [0,1] (0.05 / 0.50 / 0.95 三档)

**仍是候选 (HW-10 主路径) - 不完整**:
- 当前仅 contract test 验证 fake Jev prefilter + 3 段 confidence routing
- 缺生产代码 `JevPrefilter` 真实集成到 PromptBasedJudgmentService
- 缺 RuntimeFactSetAssembler 暴露 `source=jev_skip` 字段
- 缺 harness-config.yml `jev.prefilter` 配置段
- 缺 API_CONTRACTS.md 同步更新

**下一阶段** (pre 维护者拍板):
- HW-10 主路径实施 (对应 FEAT-03 主路径的 prefilter 部分)
- 考虑 DocsIndexAuditScriptTest sandbox fork JVM bug 隔离研究(阻断未来每个 round)
