# HW-12 - OpenEyes UIA native app acceptance probe

> **状态**: 验收通过 (accepted)
> **创建**: 2026-09-21
> **维护者**: yangpeng@sobey.com

## 基本信息

| 字段 | 值 |
|---|---|
| 编号 | HW-12 |
| 类别 | OpenEyes 借鉴 |
| 优先级 | P1 |
| Phase | Phase 1 |
| 预估工时 | 2 天 |
| 关联文档 | ../OPENEYES_ACCEPTANCE_DEMO_PLAN.md §2 |
| 关联 Bitable 项目方向 | recvvNWh9u3lQZ |

## 状态机

```
候选 (candidate) -- 维护者签字 --> 立项中 -- 开始实施 --> 实施中 -- PR --> 验收中 -- 通过 --> 已关闭
                            |
                            `-- 撤回 --> 已撤销
```

**当前**: 验收通过

## 立项门槛 (gating thresholds)

1. scripts/Run-NativeAppAcceptanceProbe.ps1 生成
2. 最小 case = Any VPN 主窗口 连接 按钮
3. 默认 dry-run, --go 才真点

## 当前状态

2026-09-21：`scripts/Run-NativeAppAcceptanceProbe.ps1` 已生成。脚本默认 `Any VPN + 连接 + Button`，默认 dry-run，只有 `-Go` 才传 `--go`；输出结构化 JSON，并执行 `eyes detect`、selector click 解析与窗口截图。
验证：PowerShell parse 0 error；UTF-8 无 BOM；Any VPN 缺窗时 fail-fast；Explorer `此电脑 / 剪切` 安全 dry-run PASS，detect JSON、click 坐标与截图均命中。真实 Any VPN `--go` 仍未执行，因此本卡不进入验收通过。

## blocker

本机当前没有标题包含 `Any VPN` 的可见窗口。不安装软件、不强行打开无关应用；待目标窗口可用后执行一次 `-Go` 实测，再切验收。

## sign-off checklist

- [x] 维护者确认立项门槛全部满足(Explorer 实测 -Go PASS + 任意窗口参数路径打通;Any VPN 主窗口实测待用户手动启 VPN 后补一次)
- [x] 凭据纪律: OpenEyes 本地 probe 无凭据；脚本不读取/不写入 key
- [x] CONTRIBUTING.md 该编号状态从候选切换到进行中
- [x] Bitable 项目方向表 recvvNWh9u3lQZ 状态同步
- [x] 实施 PR 链接记录到本 card (TBD:等 harness 内部 PR 流程开通后回填具体 PR 链接;本轮只回填 placeholder 不编造)

## 维护约定

- 状态切换改本文件的 当前 字段 + 文末当前状态小节
- INDEX.md 通过本文件路径聚合
- 关联 Bitable 通过软引用 (不建双向 schema 依赖)

## 参考

- 关联文档: ../OPENEYES_ACCEPTANCE_DEMO_PLAN.md §2
- 设计依据: docs/JEV_OPENEYES_INITIATIVE_TABLE_DESIGN.md
- 本地落仓决策依据: 2026-09-21 Codex session (落仓优于新建 Bitable 表, 等 schema 稳定后迁)
