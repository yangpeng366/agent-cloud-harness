# HW-09 - Jev x HandoffPacket keep-probability 过滤

> **状态**: 候选 (candidate)
> **创建**: 2026-09-21
> **维护者**: yangpeng@sobey.com

## 基本信息

| 字段 | 值 |
|---|---|
| 编号 | HW-09 |
| 类别 | harness 借鉴 |
| 优先级 | P1 |
| Phase | Phase 3 |
| 预估工时 | 1-2 天 |
| 关联文档 | ../JEV_CONTEXT_SCORING_PLAN.md §1.1a |
| 关联 Bitable 项目方向 | recvvNTY6alo4f |

## 状态机

```
候选 (candidate) -- 维护者签字 --> 立项中 -- 开始实施 --> 实施中 -- PR --> 验收中 -- 通过 --> 已关闭
                            |
                            `-- 撤回 --> 已撤销
```

**当前**: 候选 (合同 test 已通过)

## 立项门槛 (gating thresholds)

1. FEAT-03 已签字 (HW-09 是子集)
2. keep_probability 字段加到 ResumePacket/HandoffPacket
3. low-prob 字段降级到 ## footnotes 而不删除

## 当前状态

2026-09-22：合同 test HandoffPacketKeepProbTest PASS。合同通过 metadata (Map) 附 keep_probability，不改变 HandoffPacket record 签名。低概率项降级到 jev_footnotes 而非删除。

## blocker

(无)

## sign-off checklist

- [ ] 维护者确认立项门槛全部满足
- [ ] 凭据纪律: key 仍按 ~/.openclaw/secrets/ 凭据约定存放
- [ ] CONTRIBUTING.md 该编号状态从候选切换到进行中
- [ ] Bitable 项目方向表 recvvNTY6alo4f 状态同步
- [ ] 实施 PR 链接记录到本 card

## 维护约定

- 状态切换改本文件的 当前 字段 + 文末当前状态小节
- INDEX.md 通过本文件路径聚合
- 关联 Bitable 通过软引用 (不建双向 schema 依赖)

## 参考

- 关联文档: ../JEV_CONTEXT_SCORING_PLAN.md §1.1a
- 设计依据: docs/JEV_OPENEYES_INITIATIVE_TABLE_DESIGN.md
- 本地落仓决策依据: 2026-09-21 Codex session (落仓优于新建 Bitable 表, 等 schema 稳定后迁)

## 14. 签字 + 迁入 src/ + 全仓库共存(2026-09-21)

**签字**:维护者(Codex session 2026-09-21)签字 HW-09。
理由:
- HW-09 依赖 FEAT-03 已签字 (Jev-Graph-B path B prototype 4/4 测试绿)
- HandoffPacket 是 public record, signature 不动; keep_probability 通过 `metadata.jev_decisions` + `metadata.jev_footnotes` 追加,不破现有 contract
- 风险: append-only metadata map, Contract-Additive 路径

**迁入路径**:
- `src/test/java/com/agentcloud/scorer/HandoffPacketKeepProbTest.java` (8478 B, 4 个 JUnit 5 测试)
- 跑仓库自带 focused test: `.\scripts\Test-WithJava21.ps1 -Dtest=HandoffPacketKeepProbTest`
- 实测: `Tests run: 4, Failures: 0, Errors: 0, Time elapsed: 0.477 s -- BUILD SUCCESS`

**全仓库共存验证**:
- 排除 DocsIndexAuditScriptTest 后跑 96 个 suite / 744 tests
- 失败 suites 与 Jev-Graph-B 跑通时**完全相同** (8 个 pre-existing: DocsStructureContractTest / TaskHandlerLongStabilitySmoke / StabilitySmoke / CommandTool / CodeBuddy / CodexAppServerWorker / ProviderProtocolDiscovery)
- HW-09 不引入任何新 failure

**4 个 JUnit 测试**:
1. `keepProbAttachedPerItem`: 4 items → 2 kept verbatim + 2 demoted to footnotes, total count 保持 4
2. `keepProbValuesInRange`: 所有 keep_probability 值 ∈ [0,1]
3. `threeSegmentRouting`: 3 段 confidence threshold (keep >= 0.85 / flag_for_review >= 0.5 / demote_to_footnotes < 0.5)
4. `handoffPacketMetadataJsonRoundTrip`: HandoffPacket.metadata 含 jev_decisions + jev_footnotes, Jackson 序列化往返保形

**仍是候选 (HW-09 主路径) - 不完整**:
- 当前仅 contract test 验证 fake Jev 评分 + metadata Map 序列化
- 缺生产代码 `HandoffPacketBuilder.build()` 真实集成 JevScorer
- 缺 `RuntimeFactSetAssembler` 暴露 `jev_call_count` + `jev_latency_ms_p50` 等观测字段
- 缺 harness-config.yml `jev.context_scoring.keep_threshold` 配置项
- 缺 API_CONTRACTS.md 同步更新

**下一阶段** (pre 维护者拍板):
- HW-09 主路径实施(对应 `INITIATIVES/Jev-Graph-A` 或 `FEAT-03` 主路径,需 schema + production 代码)
