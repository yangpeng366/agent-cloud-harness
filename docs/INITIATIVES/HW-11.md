# HW-11 - OpenEyes CDP profile 持久化接入 acceptance 套件

> **状态**: 验收通过 (acceptance passed)
> **创建**: 2026-09-21
> **维护者**: yangpeng@sobey.com

## 基本信息

| 字段 | 值 |
|---|---|
| 编号 | HW-11 |
| 类别 | OpenEyes 借鉴 |
| 优先级 | P1 |
| Phase | Phase 1 |
| 预估工时 | 1 天 |
| 关联文档 | ../OPENEYES_ACCEPTANCE_DEMO_PLAN.md §1 |
| 关联 Bitable 项目方向 | recvvNWh9u3lQZ |

## 状态机

```
候选 (candidate) -- 维护者签字 --> 立项中 -- 开始实施 --> 实施中 -- PR --> 验收中 -- 通过 --> 已关闭
                            |
                            `-- 撤回 --> 已撤销
```

**当前**: 验收通过

## 立项门槛 (gating thresholds)

1. [x] scripts/Run-OpenEyesProfileSmoke.ps1 生成
2. [x] --seed 与 --no-seed 双路径验证
3. [x] 5 次 acceptance 跑中 PROFILE_PRESERVED 5/5

## 当前状态

验收通过：standalone smoke、现有 Dialogue probe 可选集成、5 次 no-seed acceptance 与 fresh direct 对照均已闭环。PR/Bitable 留痕待交接。

## blocker

(无)

## sign-off checklist

- [x] 维护者确认立项门槛全部满足
- [x] 凭据纪律: OpenEyes 本地无外部 key；本轮无凭据落仓
- [x] CONTRIBUTING.md 该编号状态切换为 implemented
- [ ] Bitable 项目方向表 recvvNWh9u3lQZ 状态同步
- [ ] 实施 PR 链接记录到本 card

## 维护约定

- 状态切换改本文件的 当前 字段 + 文末当前状态小节
- INDEX.md 通过本文件路径聚合
- 关联 Bitable 通过软引用 (不建双向 schema 依赖)

## 参考

- 关联文档: ../OPENEYES_ACCEPTANCE_DEMO_PLAN.md §1
- 设计依据: docs/JEV_OPENEYES_INITIATIVE_TABLE_DESIGN.md
- 本地落仓决策依据: 2026-09-21 Codex session (落仓优于新建 Bitable 表, 等 schema 稳定后迁)


## 14. Step 1 · 2026-09-21

**状态**: 实施中；HW-11 smoke 三条门槛实测通过，现有 acceptance probe 的登录态集成尚未收口。

**落地**:
1. `scripts/Run-OpenEyesProfileSmoke.ps1` 通过 OpenEyes CLI 启动 Edge CDP，支持 seed/no-seed、重复次数、headless、独立 profile 与 JSON 证据输出。
2. seed + 5 次 no-seed cycle 输出 `PROFILE_PRESERVED seed=1` 与 `PROFILE_PRESERVED run=1..5/5`，汇总 `status=PASS`、`profile_preserved_count=5/5`。
3. 单独 `-NoSeed` 复用路径输出 `PROFILE_PRESERVED run=1/1`，证明持久化不依赖同一次 seed 命令。
4. 当前 persistence marker 是 profile 复用的结构化判据；后续接入 `Run-DialogueBrowserAcceptanceProbe.ps1` 时再补 `user_session.cookies_loaded` 与登录耗时对比。



## 15. Step 2 · 2026-09-21

**状态**: 现有 acceptance probe 已增加 `UseOpenEyesProfile` 可选层；默认关闭时沿用 direct Edge 路径。

**合同**:
1. `-UseOpenEyesProfile` 使用 `~/.openeyes/profiles/dialogue-acceptance`，首次 seed，后续 no-seed。
2. `-OpenEyesNoSeed` 强制 no-seed；marker 缺失时 fail-fast，避免静默 fresh profile。
3. both 模式下 chat 与 responses 共用同一 OpenEyes profile；输出新增 `openeyes_profile_enabled / dir / seed_mode`。
4. 2026-09-21 本机 harness health 不可达，因此本轮验证 PowerShell parse 0 error 与 standalone OpenEyes launch 5/5；完整 cookies_loaded / 登录耗时验收待 harness 启动后补跑。


## 16. Step 3 · 2026-09-21

**状态**: acceptance 通过。

**验证**:
1. 当前工作树 `mvn clean package -DskipTests` 通过，18080 harness `/api/v1/health` 返回 `status=up`。
2. OpenEyes profile seed + UI seam chat acceptance 通过，marker 在 CDP 启动成功后立即落盘。
3. 5 次跨命令 `-OpenEyesNoSeed` acceptance 全部 `exit_code=0 / seed_mode=no-seed / surface=chat_completions / lifecycle_mode=ui_seam`。
4. 全新 direct Edge profile 对照 acceptance 通过，`openeyes_profile_enabled=false / openeyes_seed_mode=direct`。
5. 旧 direct profile 的一次失败发生在 manual receipt 时序，fresh profile 复跑通过；与 OpenEyes 层无关。
